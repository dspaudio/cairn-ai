# cairn-upgrade

Update the Cairn installation from the current source.

Upgrade must run from the package/global source because the cached installation is replaced. Do not invoke upgrade from the cached plugin locator.

```sh
bunx cairn-ai@latest upgrade
```
