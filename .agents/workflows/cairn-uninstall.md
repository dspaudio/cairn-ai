# cairn-uninstall

목표: Cairn이 설치한 Codex, Claude Code, Antigravity 항목을 제거합니다.

실행:

```sh
bunx cairn-ai@latest uninstall
```

로컬 소스에서 실행할 때:

```sh
scripts/cairn uninstall
```

원본 작업 폴더와 사용자가 만든 `MEMORY.md`, `PLAN.md`, `docs/memory`, `docs/plan`은 삭제하지 않습니다.
