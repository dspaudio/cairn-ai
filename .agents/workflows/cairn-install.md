# cairn-install

목표: Cairn을 Codex, Claude Code, Antigravity에 설치합니다.

실행:

```sh
bunx cairn-ai@latest install
```

로컬 소스에서 실행할 때:

```sh
scripts/cairn install
```

설치 후 `cairn-doctor` 워크플로우로 상태를 확인합니다.
